[package]
name = "cteno-host-session-registry"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
dashmap = "=5.2.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
cteno-host-rpc-core = { path = "../cteno-host-rpc-core" }
